You are a senior backend engineer specializing in PostgreSQL + Node.js (Next.js App Router).

Task:
Fix ALL runtime errors related to missing database relations in PostgreSQL.

Current error:
- relation "homeworks" does not exist
- relation "schedules" does not exist

---

## Step 1: Inspect database usage in codebase
Search all SQL queries in:
- src/lib/db.js
- src/lib/modules/**
- src/app/**

Identify all referenced tables:
- homeworks
- schedules
- modules
- students
- any other missing relations

---

## Step 2: Verify schema consistency
For each table:
- check if it exists in migrations / schema / SQL files
- if not found, determine expected structure from usage in code

---

## Step 3: Fix missing tables (IMPORTANT)

If table DOES NOT exist in database schema:

### Option A (preferred if schema is intended)
Create PostgreSQL migration SQL for missing tables.

Example format:

CREATE TABLE IF NOT EXISTS homeworks (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  module_id INTEGER,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS schedules (
  id SERIAL PRIMARY KEY,
  title TEXT NOT NULL,
  start_time TIMESTAMP,
  end_time TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);

Rules:
- Infer columns from code usage
- Ensure foreign keys are added if relationships exist
- Use correct PostgreSQL types

---

### Option B (if table is legacy / unused)
- Remove or comment out queries referencing it
- Ensure no runtime calls still use it

---

## Step 4: Fix schema mismatch issues
Check for:
- plural vs singular mismatch (homework vs homeworks)
- schema prefix issues (public.homeworks vs homeworks)
- case sensitivity issues

Normalize all queries to match actual DB schema.

---

## Step 5: Ensure initDb stability
Fix initDb so it:
- does NOT crash if tables are missing
- logs missing tables clearly
- does not silently skip critical tables

---

## Step 6: Validate application
Ensure:
- Student pages load without DB errors
- Admin schedules page works
- Homework pages work
- No "relation does not exist" errors remain

---

## Constraints:
- DO NOT change business logic
- DO NOT change UI
- ONLY fix database schema, migrations, and SQL queries
- Prefer creating migrations over deleting features

---

## Output:
Provide:
- list of missing tables found
- SQL migrations created
- queries fixed
- files modified