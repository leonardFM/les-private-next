Analyze the hydration mismatch occurring in ProgramCard and the entire localization system.

Current issue:
The server renders Indonesian text ("Durasi") while the client renders English text ("Duration"), causing React hydration failures.

Tasks:

1. Audit all localization and language detection logic.
2. Find every occurrence of:

   * navigator.language
   * window.navigator
   * localStorage language settings
   * typeof window checks
   * browser-only locale detection
3. Identify all components where server and client may render different languages.
4. Refactor localization so that language selection is determined consistently during SSR and passed to client components via props, context, or server state.
5. Eliminate all hydration mismatches.
6. Ensure server-rendered and client-rendered content are identical during hydration.
7. Follow Next.js App Router best practices.
8. Produce production-ready code without temporary workarounds.

Deliverables:

* Root cause analysis
* List of affected files
* Applied fixes
* Hydration-safe localization implementation
